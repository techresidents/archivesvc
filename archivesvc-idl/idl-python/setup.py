#!/usr/bin/env python

from distutils.core import setup

setup(name='trarchivesvc',
      version='${project.version}',
      description='Tech Residents Service',
      packages=['trarchivesvc',
                'trarchivesvc.gen']
    )

